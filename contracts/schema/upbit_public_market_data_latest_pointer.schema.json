{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "trader1.upbit_public_market_data_latest_pointer.v1",
  "title": "upbit_public_market_data_latest_pointer",
  "type": "object",
  "required": [
    "schema_id",
    "generated_at_utc",
    "project_id",
    "collector_id",
    "exchange",
    "market_type",
    "mode",
    "session_id",
    "symbol",
    "report_path",
    "report_hash",
    "public_market_data_hash",
    "canonical_event_count",
    "quarantine_path",
    "live_order_ready",
    "live_order_allowed",
    "can_live_trade",
    "scale_up_allowed"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_id": {"const": "trader1.upbit_public_market_data_latest_pointer.v1"},
    "generated_at_utc": {"type": "string"},
    "project_id": {"const": "TRADER_1"},
    "collector_id": {"type": "string", "minLength": 1},
    "exchange": {"const": "UPBIT"},
    "market_type": {"const": "KRW_SPOT"},
    "mode": {"const": "PAPER"},
    "session_id": {"type": "string", "minLength": 1},
    "symbol": {"type": "string", "minLength": 1},
    "report_path": {"type": "string", "minLength": 1},
    "report_hash": {"type": "string", "minLength": 64, "maxLength": 64},
    "public_market_data_hash": {"type": "string", "minLength": 64, "maxLength": 64},
    "canonical_event_count": {"type": "integer", "minimum": 0},
    "quarantine_path": {"type": ["string", "null"]},
    "live_order_ready": {"const": false},
    "live_order_allowed": {"const": false},
    "can_live_trade": {"const": false},
    "scale_up_allowed": {"const": false}
  }
}
